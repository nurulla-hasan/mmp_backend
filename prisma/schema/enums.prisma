enum Role {
  USER
  SURVEYOR
  ADMIN
  SUPER_ADMIN
}

enum ActiveStatus {
  ACTIVE
  BLOCKED
}

enum AuthProvider {
  GOOGLE
  CREDENTIAL
}

enum UserStatus {
  ACTIVE
  BLOCKED
}

enum VerificationStatus {
  PENDING
  APPROVED
  REJECTED
}
